# Middleware

This directory will contain Express middleware functions.

Middleware runs BEFORE your route handler. Common uses:
- Authentication (is this user logged in?)
- Rate limiting (is this user sending too many requests?)
- Request validation (is the request body valid?)
- Logging (log every request for debugging)
