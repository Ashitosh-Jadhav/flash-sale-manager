# Utils

This directory will contain utility/helper functions.

These are pure functions that don't depend on Express, databases, or external services.

Examples: formatting dates, generating unique IDs, validation helpers.
